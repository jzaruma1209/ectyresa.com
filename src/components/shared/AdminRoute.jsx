/**
 * Guard de ruta para el panel de administración.
 * DESHABILITADO temporalmente para desarrollo.
 * TODO: Restaurar validación de autenticación y rol cuando el backend esté listo
 */

// eslint-disable-next-line no-unused-vars
const AdminRoute = ({ children }) => {
  return children;
};

export default AdminRoute;
